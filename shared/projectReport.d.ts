import type { DaruWorkOSState, ProjectCard } from '../src/types';
export function generateProjectNewsReport(state: DaruWorkOSState, now?: Date): string;
export function generateSingleProjectReport(project: ProjectCard, now?: Date): string;
